# TimeTrack2 UI 功能契约（v1.0 · 2026-08-16）

**用途**：本文档是 TimeTrack2 UI 设计的唯一输入。它规定产品"必须做什么"——行为、交互、数据语义、技术边界；不规定"长什么样"。色彩、字体、间距、圆角、阴影、图标风格、组件形态、布局尺寸等一切视觉决策由设计方自行推导，并为每个视觉决策给出一条理由。

**自包含声明**：TimeTrack2 是从零重建的产品，不存在任何"上一版 UI"。本文不含、也不得假设任何历史视觉参照。

**阅读规则**：
1. 优先级：§2 不变式 > §4–§7 功能规格 > §8 技术约束 > §9 已拍板决策；冲突时前者胜。
2. 发现本文内部矛盾、或某要求在你的设计中不可实现时：按原样保留该条目，在 §10 对照清单中标"矛盾"并说明，不得静默取舍。
3. "须 / 必须 / 不得"为硬约束；"可 / 建议"为自由度。
4. 用户可见文案一律用中文占位（产品最终中英双语，本地化由工程层处理，设计不需管理键名）。

## 1. 产品定位与全局情境

- **产品**：离线优先的个人时间追踪工具。核心循环：开始/停止计时 → 回看与编辑时间条目 → 按活动/层级分类聚合统计；可选能力：云同步多设备、LAN 设备互通、后台自动记录（按规则自动计时）、AI 总结（二期）。
- **平台**：仅 Windows 桌面 + Android。无 iOS / macOS / Web。
- **双情境**：桌面 = 长时间驻留使用（托盘、后台记录、键盘效率、信息密度可偏高）；移动 = 单手操作、一瞥可读。同一产品，同一套数据语义。
- **自适应三档**（逻辑像素）：< 600 紧凑 / 600–840 中宽 / ≥ 840 宽屏。任何功能三档均须可达，不允许某能力只存在于某一档。
- **深浅色**：双主题同规格成立。默认浅色，可切深色/跟随系统。深色主题不是浅色的附注，须完整推导（含活动色板在深色底上的可读性策略）。
- **落地节奏**（设计应全覆盖，工程分期实现）：一期 = 应用壳、计时、今日、时间线、设置、统计、弹窗体系、后台自动记录、更新；二期 = AI（总结与自然语言记录，一期仅预留入口位）。

## 2. 全局行为不变式（最高优先级，任何页面不得违反）

1. **离线优先**：读写即时落本地，云/LAN 同步异步进行。任何页面不得因网络等待阻塞；同步状态须可感知（设置页摘要 + 必要的轻量状态指示）。
2. **软删除 + 删除可撤销**：一切删除进撤销栈；删除分类 = 递归软删子孙分类及活动关联，在撤销栈中必须是**一条**记录（可读标签如"删除分类"），不得拆成多条。
3. **时间条目携带活动名/色快照**：历史条目的展示（列表、时间线）用发生时刻的活动名与颜色快照，活动后续改名/改色不改写历史；统计聚合按当前归属计算。
4. **跨天拆分 + 运行中条目跨日滚转**：跨 0 点仍运行的会话自动滚转为当日条目；编辑产生跨 0 点区间时须拆分处理（见 §4.3 编辑器）。
5. **唯一"未分配"活动 + 相邻未分配条目自动合并**：系统有且仅有一个"未分配"活动；时间线上与之相邻的未分配条目按可配阈值自动合并（阈值见 §4.5）。
6. **统一指令分发**：UI 按钮、快捷键、深链、后台自动记录、AI 全部经同一指令通道，一切操作自动获得撤销/重做、操作日志、云同步。对 UI 的含义：操作结果反馈（成功/失败 + 可撤销）是系统级行为，各页面不得自行发明反馈语义。
7. **dataRevision 驱动刷新**：任何数据变更（尤其分类变更）后，所有相关页面即时一致，无手动刷新。
8. **自动条目标记（is_auto）**：后台自动记录产生的条目须可识别——列表/时间线有区分标识；统计可整体排除；可批量清理；编辑自动条目须有防误编辑保护（如二次确认）。
9. **更新安全**：下载校验一致才安装；失败逐层降级，最终兜底浏览器；强制更新不可跳过（见 §7）。
10. **LAN 主机仅手动启动**：任何 UI 不得自动开启 LAN 主机。

## 3. 信息架构与应用壳

### 3.1 五主页面

计时（默认着陆）/ 今日 / 时间线 / 统计 / 设置。

- 不设独立"首页"与"报告"页：今日页在宽屏承担首页职责，统计页即报告。
- 5 个导航目的地常驻可达、可辨识（图标 + 文字标签）。
- 页面间切换保持各页自身状态（壳保活，不重置滚动位置/筛选条件）。

### 3.2 导航形态（结构已定，视觉自由）

- 宽屏（≥840）侧向导航；中宽/紧凑底部导航；紧凑档允许"导航 + 计时条"底部两层叠置。
- 应用名/品牌在壳上可见。

### 3.3 全局计时条（常驻底部，贯穿所有页面）

- 运行中：当前活动名 + 实时计时（秒级推进）+ 回到计时页的入口；可从任意页面发起"切换活动"。
- 未运行：占位状态，弱化呈现"未在记录"。
- 紧凑档下空间不足时，次要信息可隐藏，活动名与计时不得省略。

### 3.4 撤销/重做（全局）

- 桌面：常驻可达入口，不可用时禁用并以动作名提示。
- 移动：收纳为可达菜单（弹层/抽屉），菜单项显示动作名。
- 快捷键 Ctrl+Z / Ctrl+Y；输入框聚焦时让位给文本编辑。
- 不可安全撤销（冲突校验不过）时禁用并提示原因，不得静默失败。

### 3.5 全局反馈调度

被动弹窗/横幅由壳层统一调度（决策规则见 §5.2）；页面不得各自为政弹出系统级提示。

## 4. 逐页功能规格

### 4.1 计时页

- 当前会话为页面焦点：运行中活动（色 + 名）、实时时长、今日累计时长、今日会话数。
- 操作：**停止**（结束当前会话）；**切换活动**（结束当前 + 开始新的，一个原子操作，不产生中间空档）。
- 快捷活动区：快速启动/切换常用活动；固定包含"临时活动"（免配置直接记）与"新增活动"两个入口；正在运行的活动有区分态。
- 快捷区已定交互：单击选中/再击确认切换（选中待确认的中间态须可见）；双击直接切换；长按（桌面为悬停可见的编辑入口）打开活动编辑。
- 活动选择与分类树集成：一切"选活动"场景复用 §5.1 合并选择交互。
- 三态：加载 / 空（无任何活动时引导创建）/ 错误（页面级呈现）。

### 4.2 今日页

- 查看任意日期（默认今天）：日历选择 + 前后步进。
- 指标 4 项：总时长 / 会话数 / 专注时长 / 休息时长（口径由数据层定义，按 4 项呈现），带较前一日对比。
- 分类分组聚合列表：按主分类分组（分类名 + 小计时长 + 占比），组内列活动（名 + 时长 + 占比）；组可折叠；分类层级 > 1 时组头显示祖先链路径（如"工作 / 项目A"）；未分类活动归入"未分类"组置底。
- 分类筛选：多选（分类可辨识），联动过滤列表并重算 4 项指标；默认全选。
- 宽屏含时间线预览（限约 5 条）+ "查看完整时间线"跳转；紧凑档可省略预览。
- 三态：加载 / 空（当日无记录的引导）/ 错误。

### 4.3 时间线页

- 头部：添加条目（主动入口）；日期步进（前/后/选择）；跨度切换（今天 / 三天 / 本周）；视图切换（条目 / 日志）。
- 汇总 3 项：总时长 / 会话数 / 最长连续。
- 条目呈现（两种能力并存，断点分配由设计定）：
  - **时间比例可视化**：条目按时长/时刻在时间轴上定位，含时间刻度与日期分组，点击条目开编辑。已定交互：单日模式横向滚动 + 缩放控制；分段堆叠模式每日段数可调（1–12）。
  - **条目列表流**：开始时间 + 时长、活动（名/色）、备注，可点开编辑。
- 查看未来日期时显示提示横幅（未来无记录是正常态，不是错误）。
- 自动条目（is_auto）在可视化与列表中均有区分标识。

**条目编辑对话框**（新增/编辑共用）：

- 活动选择：搜索实时过滤 + 活动候选（含"临时"标识）+ 现场新建活动（名称 + 持续/临时 + 颜色）。
- 起止时间：分别可编辑（含日期）；编辑运行中条目时提供"保持运行"开关。
- 备注：多行。
- 仅编辑已有条目：与左/右相邻条目合并（超过合并阈值时须确认）、按分割点拆分、延伸到当前时刻。
- 底部操作：删除 / 取消 / 保存。
- 校验：结束须晚于开始（阻断）；运行条目不得落在将来（阻断）；与既有条目重叠（警告，允许继续）；跨 0 点区间（提示将拆分）。
- 日志视图：操作日志流，每条含操作类型、时间、消息、设备名。
- 三态：加载 / 空（无记录引导）/ 错误。

### 4.4 统计页

- 范围：今天 / 昨天 / 本周 / 上周 / 自定义 + 日期步进。
- 维度 4 种：活动 / 主分类（**树聚合**：按祖先链归并，标签为分类路径）/ 时长区间 / 分类 × 时长。
- 分类筛选：树形呈现（层级分组/缩进）、可折叠/展开、多选。
- **树统计覆盖全屏宽**：三档断点下树维度信息完整性一致，紧凑档不得丢弃层级信息。
- 图表（fl_chart 能力边界内）：每日分布（柱状类）+ 占比分布（环形类）+ 图例 + 每日明细列表。
- 自动条目可整体排除出统计。
- AI 总结（二期功能，一期仅预留入口位）：入口在统计页头部；未配置 AI 时点击呈现引导（跳设置），**不得报错**。二期完整行为：自然语言提问输入 + 快捷建议；结果卡（文字 + 关键数据高亮）；解析出指令时"指令预览 + 确认执行"（必须确认）；结果缓存 + 重新生成；加载/失败重试。
- 三态：加载 / 空（"开始记录吧"类引导）/ 错误。

### 4.5 设置页

分区化组织（紧凑：分组列表 → 详情；宽屏：可总览 + 分区导航；具体形态自由）。所有设置即时生效并持久化，变更后相关页面即时一致（不变式 7）。

| 分区 | 内容 |
|---|---|
| 通用 | 外观（浅/深/跟随系统，默认浅色）；每周起始日；时间格式（12/24 时制）；默认记录时长；快速提醒开关 |
| 备份与导出 | 导出 `.timetrack.json` / 导入 / 清除全部数据（**清除前强制提示先导出备份**） |
| 提醒 | 触发时刻；持续时长阈值 15–180 分钟；重复间隔 5–60 分钟；方式（对话框/横幅/静音，默认对话框） |
| 时间线 | 相邻条目合并阈值 1–60 分钟 |
| 云同步 | 配置状态；登录/登出；同步状态摘要；立即同步 |
| AI 配置（二期） | 密钥（遮蔽显示、安全存储）；端点/模型（OpenAI 兼容）；测试连接；清除密钥；启用时数据出境披露 + 本地模型选项提示 |
| 后台记录 | 见 §6 |
| 设备互通 | LAN 主机（**手动**启动/停止 + 地址 + 配对码）/ LAN 客户端（地址 + 配对码配对）/ 文件导入导出 / 互通状态 |
| 版本更新 | 见 §7 |
| 关于 | 版本、开源信息 |

- 云同步登录流程：入口在云同步分区；登录界面形态自由；须含加载/失败态。

## 5. 跨页交互系统

### 5.1 活动-分类合并选择器（核心交互，无独立分类管理页）

数据语义：分类为层级树（父子嵌套，无深度硬限制）；每个活动有 1 个主分类 + 多个副分类；活动可无分类。一切"选活动"的场景（计时页切换、条目编辑、后台规则目标选择）复用同一选择交互：

- 两形态（已定，视觉自由）：桌面 = 弹窗内左侧分类树（可展开/缩进/分类可辨识）+ 右侧该分类及子孙分类下活动集合 + 顶部跨分类搜索 + 底部"新建分类"与"新建活动"入口；移动 = 底部抽屉：搜索 → 分类筛选行（含"全部"）→ 活动集合。
- 选分类即过滤活动；选活动即完成选择。
- 新建/编辑活动内嵌完成：名称 + 颜色 + 持续/临时 + 主/副分类选择。
- 新建/编辑/**递归删除**分类内嵌同一界面；删除须确认（明确提示将递归软删子孙分类及其活动关联）；撤销为一条记录（不变式 2）。
- 空分类显示"新建分类"入口；系统无任何分类时退化为"全部活动"模式。
- 分类变更实时刷新选择器（不变式 7）。
- 长按/右键分类节点可管理（含删除入口）。
- 层级展示：树深 > 3 层的部分以祖先链路径/面包屑呈现，不做无限层级平铺。

### 5.2 弹窗体系（决策规则 + 场景分配）

决策规则：

- 需用户做**不可逆 / 二选一**决策 → 对话框（桌面居中、移动底部抽屉）；
- 纯通知 → Snackbar（浮动、单条）；
- 常驻提醒 → 横幅。

场景分配（已定）：

| 场景 | 载体 | 操作 |
|---|---|---|
| 计时提醒"仍在进行？" | 对话框 | 稍后提醒 / 停止 / 继续（主） |
| 运行 N 分钟提醒 | 横幅或 Snackbar | 稍后提醒 / 关闭 |
| 可疑条目（启动时检测到遗留运行条目） | 对话框（须决策） | 保留当前 / 结束到现在（主） |
| 版本更新可用 | Snackbar | 查看（跳设置）/ 忽略 |
| 强制更新 | 对话框（不可跳过、无关闭） | 立即更新 |
| 操作成功/失败 | Snackbar | 可带"撤销" |

### 5.3 撤销/重做

见 §3.4。撤销单元划分以指令通道为准（一条指令 = 一条撤销记录），UI 不自造粒度。

## 6. 后台自动记录（一期晚段）

### 6.1 规则模型

规则 = 匹配模式（进程名 / 窗口标题，支持精确与通配）→ 目标活动。匹配优先级：精确 > 通配 > 标题（须在 UI 中说明）。每条规则独立云同步开关（默认开）。未匹配任何规则时不产生条目。

### 6.2 设置 → 后台记录分区

- 授权状态卡：Android = "使用情况访问"已授权/未授权 + 跳系统设置按钮（该权限**不能**运行时弹窗，只能跳系统设置页）；Windows = 常驻说明条 + 检测状态。
- 总开关：未授权时禁用并引导。
- 规则列表：每条规则呈现匹配模式、匹配类型、目标活动（色 + 名）、云同步开关、启停开关、编辑、删除。
- 新建/编辑规则表单：模式输入 + 匹配类型二选一（进程名/窗口标题）+ 目标活动选择（复用 §5.1）+ 云同步开关 + 优先级说明。

### 6.3 授权引导（Android 首次进入）

全屏引导：为何需要此权限 + 隐私承诺（不读屏幕内容）+ "去系统设置开启" + "暂不开启"（返回且功能关闭，不反复纠缠）。Windows 不设引导页。

### 6.4 平台驻留

- Windows 托盘：关闭窗口默认最小化到托盘（首次关窗弹选择对话框，可记住选择）；托盘左键 = 打开主窗口，右键菜单 = 显示主窗口 / 暂停记录 / 退出；托盘提示显示当前记录状态（如"正在记录：X"）。
- Android 前台服务常驻通知：低优先级、不可清除；标题"正在记录"，内容 = 当前前台应用 → 命中规则的活动（无命中时"检测中…"）；点击回主界面；带"暂停"动作。
- 自动记录与手动计时互不抢占；自动条目带 is_auto（不变式 8）。

## 7. 更新系统

- 状态机（设置页更新卡须完整呈现）：idle / checking / upToDate / available / failed / downloading（进度）/ paused / verifying / installing / restartRequired。
- 更新卡：当前版本 + 最新版本 + 状态 + 手动检查更新 / 下载进度（进度 + 速度 + 可取消）/ "忽略此版本"（持久化）/ "稍后提醒"。
- 启动静默检查：发现新版本仅以 Snackbar 轻提示，不打断当前操作。
- 强制更新：不可跳过的对话框，唯一操作"立即更新"。
- 失败降级：逐层降级，最终兜底"浏览器打开下载页"。
- 安装差异：Windows = 校验通过后提示"重启后生效"（下次启动时应用）；Android = 拉起系统安装器，未授权"安装未知应用"时引导系统设置。

## 8. 技术与实现约束

- Flutter 3.44 / Material 3 组件基座；图表 fl_chart；文件选择 file_selector。
- 平台仅 Windows + Android。
- 实时计时数字须稳定不跳动（等宽/表格数字）。
- 桌面键盘可达：核心操作全部键盘可达；至少 Ctrl+Z / Ctrl+Y；深链入口预留。
- 组件无业务状态（参数注入、可独立预览）；页面代码落位 `lib/pages`，公共组件落位 `lib/components`。
- 一切用户可见文案中英双语（设计稿中文占位即可）。
- 三档断点（§1）驱动布局，不允许只设计单一宽度。
- 数据语义（软删/快照/LWW/is_auto）见 §2；设计不得发明与之冲突的呈现（如"彻底删除、不可恢复"类操作）。

## 9. 已拍板决策（默认值生效，不再重开）

1. 无独立首页；今日页宽屏承担。
2. 分类树选择器桌面/移动两形态（§5.1）；树深 > 3 层用祖先链呈现。
3. AI 入口一期仅统计页一处；AI 产出指令必须用户确认后才执行。
4. Windows 关窗默认最小化到托盘。
5. 提醒方式默认对话框。
6. 后台记录规则默认进云同步（per-rule 可关）。
7. 默认浅色主题，可切深色/跟随系统。
8. LAN 主机仅手动启动。
9. 平台仅 Windows + Android。

## 10. 契约对照清单（设计方逐条标注 ✓ / ✗ / 矛盾，随提案返回）

**不变式（§2）**
- [ ] 离线优先，页面不因网络阻塞，同步状态可感知
- [ ] 删除分类 = 递归 + 单条撤销记录，删除入口有确认与范围提示
- [ ] 历史条目按快照展示，统计按当前归属
- [ ] 跨天滚转与跨天编辑拆分在 UI 有对应处理
- [ ] "未分配"唯一 + 相邻合并（阈值可配）可被用户理解
- [ ] 操作反馈（成功/失败/可撤销）系统级一致
- [ ] 数据变更后所有页面即时一致
- [ ] is_auto 条目可识别、统计可排除、可批量清理、编辑有防误保护
- [ ] 强制更新不可跳过；更新失败有浏览器兜底
- [ ] LAN 主机无任何自动开启路径

**应用壳（§3）**
- [ ] 5 页导航（图标+文字），无首页/报告页
- [ ] 宽屏侧导航 / 中宽与紧凑底部导航；紧凑两层叠置可行
- [ ] 全局计时条：活动名 + 实时计时 + 回计时页 + 任意页切换活动 + 未运行占位
- [ ] 撤销/重做：桌面常驻 + 移动菜单（含动作名）+ Ctrl+Z/Y + 输入框让位 + 不可用时禁用并提示

**计时页（§4.1）**
- [ ] 会话焦点（活动/实时时长/今日累计/会话数）
- [ ] 停止 + 原子切换
- [ ] 快捷区含"临时活动"与"新增活动"固定入口，运行中有区分态
- [ ] 单击选中/再击确认（中间态可见）、双击直切、长按/悬停编辑
- [ ] 三态

**今日页（§4.2）**
- [ ] 任意日期查看（日历 + 步进）
- [ ] 4 指标 + 较前日对比
- [ ] 分类分组聚合：折叠、祖先链路径、"未分类"置底、组内活动明细
- [ ] 分类多选筛选联动列表与指标
- [ ] 宽屏时间线预览 + 跳转
- [ ] 三态

**时间线页（§4.3）**
- [ ] 添加条目 + 日期步进 + 跨度切换 + 条目/日志视图切换
- [ ] 汇总 3 项
- [ ] 时间比例可视化（刻度/日期分组/点击编辑/滚动缩放/分段 1–12 可调）
- [ ] 条目列表流（时间/活动/备注/点击编辑）
- [ ] 未来日期横幅
- [ ] 编辑对话框：搜索 + 现场新建 + 起止编辑 + 保持运行开关 + 备注 + 合并（阈值确认）/拆分/延伸 + 删除/取消/保存 + 4 类校验
- [ ] 日志视图 4 字段
- [ ] is_auto 标识；三态

**统计页（§4.4）**
- [ ] 范围 5 预设 + 步进
- [ ] 维度 4 种，含主分类树聚合（路径标签）
- [ ] 树形分类筛选（折叠/展开/多选）
- [ ] 树统计三档断点信息完整（全屏宽）
- [ ] 柱状 + 环形 + 图例 + 每日明细（fl_chart 内）
- [ ] 自动条目可排除
- [ ] AI 入口预留：未配置时引导不报错（二期完整交互已描述）
- [ ] 三态

**设置页（§4.5）**
- [ ] 10 分区齐全，内容逐项覆盖
- [ ] 清除全部前强制导出备份提示
- [ ] 提醒 15–180 / 5–60、合并阈值 1–60 范围呈现
- [ ] 登录流程含加载/失败态

**合并选择器（§5.1）**
- [ ] 桌面弹窗（树 + 活动集 + 搜索 + 双新建）/ 移动抽屉（搜索 + 分类行 + 活动集）
- [ ] 选分类过滤、选活动即完成
- [ ] 新建/编辑活动内嵌（名/色/持续临时/主副分类）
- [ ] 递归删除内嵌 + 确认提示 + 单条撤销
- [ ] 空分类入口、无分类退化"全部活动"
- [ ] 变更实时刷新；长按/右键管理；> 3 层祖先链呈现

**弹窗体系（§5.2）**
- [ ] 三条决策规则成立
- [ ] 6 个场景载体分配正确（含强制更新不可跳过）

**后台记录（§6）**
- [ ] 授权卡（Android 跳系统设置 / Windows 说明条）+ 未授权禁用总开关
- [ ] 规则列表字段与操作齐全（含 per-rule 同步开关）
- [ ] 规则表单（模式/类型/目标活动/同步开关/优先级说明）
- [ ] Android 首次全屏引导（含隐私承诺与"暂不开启"）
- [ ] 托盘三行为 + 状态提示；前台服务通知（映射文案/点击回主/暂停动作）

**更新（§7）**
- [ ] 10 个状态在更新卡完整呈现
- [ ] 进度 + 速度 + 取消；忽略版本持久化；稍后提醒
- [ ] 静默检查仅 Snackbar；强制更新唯一操作；降级浏览器兜底；双平台安装差异呈现

**技术（§8）**
- [ ] Material 3 + fl_chart 可实现性自评（不可实现处标"矛盾"）
- [ ] 三档断点 + 深浅色全覆盖
- [ ] 键盘可达；计时数字稳定不跳动
- [ ] 文案中文占位、无硬编码键名要求冲突
